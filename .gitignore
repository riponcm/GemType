# local AI tooling / project memory — never publish
CLAUDE.md
.projectmem/
.claude/

# store publishing playbook (kept local — contains submission details)
store/

# launch / marketing kit (kept local — drafts, not public repo content)
launch/

# build artifacts
safari/build/
xcuserdata/
*.xcuserstate
node_modules/
.DS_Store

# desktop app local files
desktop/node_modules/
desktop/app.log
desktop/package-lock.json
desktop/dist/
desktop/gemtype-app.log
desktop/assets/icon.icns
