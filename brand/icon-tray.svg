<svg width="32" height="32" viewBox="0 0 32 32" xmlns="http://www.w3.org/2000/svg">
  <!-- macOS menu-bar (tray) glyph: a TEMPLATE image — pure black on transparent
       so macOS tints it automatically (white on a dark bar, black on light).
       Just the checkmark, matching the brand mark, sized like other menu-bar
       glyphs. Rendered to trayTemplate.png / @2x. -->
  <path d="M6.5 17 L13 24 L25.5 9" fill="none" stroke="#000000"
        stroke-width="4.2" stroke-linecap="round" stroke-linejoin="round"/>
</svg>
